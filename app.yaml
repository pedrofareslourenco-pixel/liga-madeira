runtime: python38
handlers:
  - url: /static
    static_dir: .
  - url: /(.*)
    static_files: \1
    upload: .*

# Optional: set automatic scaling
automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 2
